<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=0.8, maximum-scale=1">
  <title>Node.js課題2</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
  <link rel="stylesheet" href="/css/custom.css">

</head>

<body>
  <!-- //-navbar共通 -->
  <nav class="navbar has-shadow" role="navigation" aria-label="main navigation" style="max-width: 900px; margin: auto;">
    <div class="navbar-brand ">
      <a class="navbar-item is-paddingless">
        <h1 class="navbar-item is-size-3 is-paddingless" style="margin-left: 5rem;">
          Lalabel
        </h1>
      </a>
      <a role="button" class="navbar-burger " id="burgerMenu">
        <span></span>
        <span></span>
        <span></span>
      </a>
    </div>

    <div id="nav-list" class="navbar-menu">
      <div class="navbar-end">
        <div class="navbar-item" style="margin-right: 5rem;">
          <div class="buttons">
            <% if (!loggedIn) { %>
              <a class="button" href="/login">
                Login
              </a>
              <a class="button" href="/">
                Register
              </a>
            <% } %>

            <!-- ログインしている時は、
              ログアウトボタンとユーザーネームを表示 -->
            <% if (loggedIn) { %>
                <a class="button" href="/logout">
                  Logout
                </a>

                <a class="button border-less">
                  <div class="">
                    <%= `▼ ${currentUser.name}` %>
                  </div>
                </a>
                <% } %>
          </div>
        </div>
      </div>
    </div>
  </nav>

  <!-- 登録 or ログイン成功時にフラッシュメッセージを表示 -->
  <div class="flashes">
    <% if (flashMessages) { %>
      <% if (flashMessages.success) { %>
        <div class="flash success">
          <%= flashMessages.success.join(' / ') %>
        </div>
  <!-- 登録 or ログイン失敗時にフラッシュメッセージを表示 -->
        <% } else if (flashMessages.error) { %>
          <div class="flash error">
            <%= flashMessages.error.join(' / ') %>
          </div>
          <% } %>
            <% } %>
  </div>

  <section class="section">
    <%- body %>
  </section>
  <!-- 小さい画面時のハンバーガーメニューが
    動くようにjsファイルを読み込んでいます -->
  <script src="/js/index.js"></script>
</body>


</html>
